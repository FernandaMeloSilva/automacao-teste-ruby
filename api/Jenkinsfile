pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('prep') {
            steps {
                sh 'cd api && bundle install'
            }
        }
        stage('Testing') {
            steps {
                sh 'cd api && rspec'
            }
        }
    }
}
